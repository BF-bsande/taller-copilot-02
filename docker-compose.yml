services:
  backend:
    build:
      context: ./backend
    container_name: jwt-fastapi-backend
    ports:
      - "8000:8000"
    environment:
      JWT_SECRET_KEY: super-secret-key-with-at-least-32chars
